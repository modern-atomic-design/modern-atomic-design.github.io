image: node:16
cache:
  paths:
    - node_modules/

before_script:
  - yarn install

pages:
  stage: deploy
  script:
    - yarn generate
    - yarn generate:storybook
    - mv storybook public/storybook
  tags:
    - build
  artifacts:
    paths:
      - public
    expire_in: 1 week
  only:
    - main
