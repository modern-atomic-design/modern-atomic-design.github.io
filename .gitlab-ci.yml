image: node:16
cache:
  paths:
    - node_modules/

before_script:
  - yarn install

pages:
  stage: deploy
  script:
    - yarn build
    - yarn generate
    - rm -rf public
    - mv dist public
  tags:
    - build
  artifacts:
    paths:
      - public
    expire_in: 1 week
  only:
    - main
